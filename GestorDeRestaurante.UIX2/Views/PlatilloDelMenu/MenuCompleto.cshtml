@model GestorDeRestaurante.Model.MenuCompleto

@{
    ViewData["Title"] = "MenuCompleto";
}

<center>
    <i>
        <h1> <p style="color:#C70039";>Menú completo</p></h1>
    </i>
</center>

<table class="table" border="1">
  
  <tr style="background: rgba(128, 255, 0, 0.3); border: 1px solid rgba(100, 200, 0, 0.3);">
    <th>Entradas</th>
    <th>Platos principales</th>
  </tr>

  <tr>

    <td id="nested">
      <table class="table" border="1">
        <tr>
          <td>Imágen</td>
          <td>Nombre</td>
          <td>Precio</td> 
        </tr>
      
        
          @foreach (var item in Model.Entradas)
            {<tr>
                <td>
                    @{
                        var base64 = Convert.ToBase64String(item.Imagen);
                        var imgsrc = string.Format("data:image/jpg;base64,{0}", base64);
                    }
                    
                    <img src="@imgsrc" style="height:50px;" class="d-block w-50" />


                </td>
                 <td>
                @Html.DisplayFor(modelItem => item.Nombre)
                 </td>
                  <td>
                @Html.DisplayFor(modelItem => item.Precio)
                </td>
             </tr>
          }
       
   
      </table>
    </td>


      <td id="nested">
      <table class="table" border="1">
        <tr>
          <td>Imágen</td>
          <td>Nombre</td>
          <td>Precio</td>
        </tr>
        @foreach (var item in Model.PlatosPrincipales)
            {<tr>
                <td>
                    @{
                        var base64 = Convert.ToBase64String(item.Imagen);
                        var imgsrc = string.Format("data:image/jpg;base64,{0}", base64);
                    }
                    
                    <img src="@imgsrc" style="height:50px;" class="d-block w-50" />


                </td>
                 <td>
                @Html.DisplayFor(modelItem => item.Nombre)
                 </td>
                  <td>
                @Html.DisplayFor(modelItem => item.Precio)
                </td>
             </tr>
          }
      </table>
    </td>
    
  </tr>

   <tr style="background: rgba(128, 255, 0, 0.3); border: 1px solid rgba(100, 200, 0, 0.3);">
    <th>Pequeñas botanas</th>
    <th>Postres</th>
  </tr>

    <tr>

    <td id="nested">
      <table class="table" border="1">
        <tr>
          <td>Imágen</td>
          <td>Nombre</td>
          <td>Precio</td>
        </tr>
         @foreach (var item in Model.PequeñasBotanas)
            {<tr>
                <td>
                    @{
                        var base64 = Convert.ToBase64String(item.Imagen);
                        var imgsrc = string.Format("data:image/jpg;base64,{0}", base64);
                    }
                    
                    <img src="@imgsrc" style="height:50px;" class="d-block w-50" />


                </td>
                 <td>
                @Html.DisplayFor(modelItem => item.Nombre)
                 </td>
                  <td>
                @Html.DisplayFor(modelItem => item.Precio)
                </td>
             </tr>
          }
   
      </table>
    </td>


      <td id="nested">
      <table class="table" border="1">
        <tr>
          <td>Imágen</td>
          <td>Nombre</td>
          <td>Precio</td>
        </tr>
        @foreach (var item in Model.Postres)
            {<tr>
                <td>
                    @{
                        var base64 = Convert.ToBase64String(item.Imagen);
                        var imgsrc = string.Format("data:image/jpg;base64,{0}", base64);
                    }
                    
                    <img src="@imgsrc" style="height:50px;" class="d-block w-50" />


                </td>
                 <td>
                @Html.DisplayFor(modelItem => item.Nombre)
                 </td>
                  <td>
                @Html.DisplayFor(modelItem => item.Precio)
                </td>
             </tr>
          }
   
      </table>
    </td>
    
  </tr>


     <tr style="background: rgba(128, 255, 0, 0.3); border: 1px solid rgba(100, 200, 0, 0.3);">
    <th>Aperitivos</th>
    <th>Bebidas</th>
  </tr>

    <tr>

    <td id="nested">
      <table class="table" border="1">
        <tr>
           <td>Imágen</td>
          <td>Nombre</td>
          <td>Precio</td>
        </tr>
         @foreach (var item in Model.Aperitivos)
            {<tr>
                <td>
                    @{
                        var base64 = Convert.ToBase64String(item.Imagen);
                        var imgsrc = string.Format("data:image/jpg;base64,{0}", base64);
                    }
                    
                    <img src="@imgsrc" style="height:50px;" class="d-block w-50" />


                </td>
                 <td>
                @Html.DisplayFor(modelItem => item.Nombre)
                 </td>
                  <td>
                @Html.DisplayFor(modelItem => item.Precio)
                </td>
             </tr>
          }
   
      </table>
    </td>


      <td id="nested">
      <table class="table" border="1">
        <tr>
          <td>Imágen</td>
          <td>Nombre</td>
          <td>Precio</td>
        </tr>
        @foreach (var item in Model.Bebidas)
            {<tr>
                <td>
                    @{
                        var base64 = Convert.ToBase64String(item.Imagen);
                        var imgsrc = string.Format("data:image/jpg;base64,{0}", base64);
                    }
                    
                    <img src="@imgsrc" style="height:50px;" class="d-block w-50" />


                </td>
                 <td>
                @Html.DisplayFor(modelItem => item.Nombre)
                 </td>
                  <td>
                @Html.DisplayFor(modelItem => item.Precio)
                </td>
             </tr>
          }
   
      </table>
    </td>
    
  </tr>
     <tr style="background: rgba(128, 255, 0, 0.3); border: 1px solid rgba(100, 200, 0, 0.3);">
    <th>Sopas y ensaladas</th>

  </tr>
    <tr>

    <td id="nested">
      <table class="table" border="1">
        <tr>
          <td>Imágen</td>
          <td>Nombre</td>
          <td>Precio</td>
        </tr>
         @foreach (var item in Model.SopasYEnsaladas)
            {<tr>
                <td>
                    @{
                        var base64 = Convert.ToBase64String(item.Imagen);
                        var imgsrc = string.Format("data:image/jpg;base64,{0}", base64);
                    }
                    
                    <img src="@imgsrc" style="height:50px;" class="d-block w-50" />


                </td>
                 <td>
                @Html.DisplayFor(modelItem => item.Nombre)
                 </td>
                  <td>
                @Html.DisplayFor(modelItem => item.Precio)
                </td>
             </tr>
          }
   
      </table>
    </td>

  </tr>



</table>
